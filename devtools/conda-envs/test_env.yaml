name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - quantities
  - numpy
  - MDAnalysis

    # Pip-only installs
  #- pip:
  #  - codecov

