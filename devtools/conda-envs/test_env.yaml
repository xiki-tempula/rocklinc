name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - quantities
  - numpy
  - MDAnalysis
  - apbs

    # Pip-only installs
  #- pip:
  #  - codecov

